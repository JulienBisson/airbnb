<div class="container_image_show">

  <div class="animal_info mb-3">
    <%=cl_image_tag @animal.photo.key, class:"image_show"%>
    <div class="card-body">
      <h5 class="card-title"><%= @animal.name %></h5>
      <p class="card-text"><%= @animal.summary %></p>
      <p class="card-text"><small class="text-muted"><%= @animal.localisation %></small></p>
    </div>
  </div>

  <div class="card-show">
    <h5 class="card-show-title"><%= @animal.price_per_day.to_i %> € </h5>
    <div class='checkin'>
      <%= simple_form_for(@animal @booking) do |f| %>
        <%= f.input :name %>
        <%= f.input :address %>
        <%= f.input :rating %>
        <%= f.submit %>
      <% end %>
    </div>
      <div class= "button-show">
        <a href="#" class="button-animal-show">Reserve</a>
      </div>
  </div>

</div>
